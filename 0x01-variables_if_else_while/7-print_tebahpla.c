#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * A program that prints the lowercase alphabet in reverse
 * Return : 0
 */

int main(void)
{
	char l;

	for(l='z'; low>='a'; l--){
		putchar(l);
		putchar('\n');
	}

	return (0)
}
